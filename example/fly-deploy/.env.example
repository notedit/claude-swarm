# ── Fly.io ────────────────────────────────────────────────────────────────────
# API token from `flyctl tokens create`
FLY_API_TOKEN=your_fly_api_token

# Fly app name for agent machines (must match fly.agent.toml)
FLY_APP_NAME=claude-agent-app

# Agent Docker image (pushed to Fly registry)
FLY_AGENT_IMAGE=registry.fly.io/claude-agent-app:latest

# ── Redis ─────────────────────────────────────────────────────────────────────
# For local docker-compose use redis://redis:6379
# For Fly.io use an Upstash or Fly Redis URL
REDIS_URL=redis://redis:6379

# ── Claude API ────────────────────────────────────────────────────────────────
# Anthropic API key — used by the Agent SDK inside each machine
ANTHROPIC_API_KEY=your_anthropic_api_key

# ── Server ────────────────────────────────────────────────────────────────────
PORT=8080
